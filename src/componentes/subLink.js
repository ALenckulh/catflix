const Sublink = ({text}) => {
    return (
        <a href="#" style={{backgroundColor:"#181410", textDecoration:"underline"}} className="font-mono text-sm font-light text-gray-500 hover:underline">{text}</a>
    )
}

export default Sublink