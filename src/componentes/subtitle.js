const Subtitle = ({text}) => {
    return (
        <h2 style={{paddingBottom:"0.5rem", paddingTop:"1.3rem", color:"#fff"}} className="font-mono block mb-2 text-sm font-medium text-[#fff]">{text}</h2>
    )
}

export default Subtitle